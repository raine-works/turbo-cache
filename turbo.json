{
	"$schema": "https://turborepo.org/schema.json",
	"globalPassThroughEnv": ["GITHUB_TOKEN"],
	"globalEnv": ["HOME", "NODE_ENV"],
	"tasks": {
		"deploy": {},
		"dev": {
			"persistent": true,
			"cache": false
		},
		"docker:build": {},
		"release": {
			"dependsOn": ["^release"],
			"outputLogs": "new-only"
		}
	}
}
